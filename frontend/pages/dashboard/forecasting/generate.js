import React from 'react';
import Generate from '../../../components/Generate';

const GeneratePage = () => {
    return (
        <div>
            <Generate />
        </div>
    );
};

export default GeneratePage;
